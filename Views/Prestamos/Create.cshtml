@{
    ViewData["Title"] = "Solicitar Préstamo";
}

<div class="container mt-4">
    <h2>📥 Solicitar Préstamo</h2>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    @if (ViewBag.LibroSeleccionado != null)
    {
        <div class="alert alert-info">
            <strong>Libro seleccionado:</strong> @ViewBag.LibroSeleccionado.Titulo - @ViewBag.LibroSeleccionado.Autor
        </div>
    }

    <form method="post">
        <div class="form-group">
            <label>Seleccionar Libro:</label>
            <select name="libroID" class="form-control" required>
                <option value="">-- Selecciona un libro --</option>
                @foreach (var libro in ViewBag.Libros)
                {
                    <option value="@libro.LibroID" 
                            selected="@(ViewBag.LibroSeleccionado?.LibroID == libro.LibroID)">
                        @libro.Titulo - @libro.Autor (ISBN: @libro.ISBN)
                    </option>
                }
            </select>
        </div>

        <div class="mt-3">
            <button type="submit" class="btn btn-primary">📥 Solicitar Préstamo</button>
            <a href="/Libros" class="btn btn-secondary">Ver Todos los Libros</a>
            <a href="/Prestamos" class="btn btn-outline-secondary">Mis Préstamos</a>
        </div>
    </form>
</div>