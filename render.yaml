services:
  - type: web
    name: biblioteca-web
    env: dotnet
    buildCommand: dotnet publish -c Release -o ./publish
    startCommand: cd ./publish && ./BibliotecaWeb
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production